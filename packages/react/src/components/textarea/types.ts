
export type StyledTextAreaProps = {
    text?: string,
    title?: string,
    label: string,
    error: boolean,
    disabled?: boolean,
    characterCounter?: boolean,
    errorMessage?: string,
    maxLength?: number,
}
